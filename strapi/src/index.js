// File: index.js
// Folder: /rwtnews/strapi/src

'use strict';

const path = require('path');

module.exports = {
  register(/*{ strapi }*/) {
    // No custom registration logic needed for this project
  },

  bootstrap(/*{ strapi }*/) {
    // No custom bootstrap logic needed for this project
  },
};